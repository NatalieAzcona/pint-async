import "./button.scss";

export const button = (text, ) => {
    const button = document.createElement('button');

    button.textContent = text;

    return button;
}

/* Tipo:

Primary
Botones s: tamaño texto, padding,
 */

